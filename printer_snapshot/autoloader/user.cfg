# Autoloader — your settings. Written once by install.sh, never overwritten.
# Included last, so anything here overrides parameters.cfg.
#
# This printer is the reference build: LEDs are enabled and its adapted
# leds.cfg lives in leds/. See docs/LEDS.md.

[include leds/*.cfg]
